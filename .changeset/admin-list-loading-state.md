---
'@tango-ts/admin-ui': patch
---

Admin list views show a loading state when switching models instead of flashing the previous model's rows, and out-of-order responses can no longer overwrite the current page.
