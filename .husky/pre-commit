# Tango pre-commit gate (DESIGN_PRINCIPLES.md §4).
# Fails the commit on any `any`, unsafe type, or type error.
echo "→ lint (no-any gate) + typecheck"
yarn lint-staged
yarn typecheck

# Serverless import-graph guard: server packages must never ship UI deps
# (react/admin-ui) that would enter the Vercel function bundle. Fast manifest
# check here; the full @vercel/nft bundle trace runs in CI.
echo "→ import-graph guard"
yarn check:import-graph
