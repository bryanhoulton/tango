# Database connection. When NODE_ENV=production these are required — Tango
# refuses to fall back to development defaults.
TANGO_DB_HOST=127.0.0.1
TANGO_DB_PORT=3307
TANGO_DB_USER=root
TANGO_DB_PASSWORD=tango
TANGO_DB_NAME=__PROJECT_DB_NAME__

# Or configure everything with one URL (overrides unset fields above):
# TANGO_DATABASE_URL=mysql://user:password@host:3306/__PROJECT_DB_NAME__?ssl=true

# TLS for managed MySQL (PlanetScale, RDS, Aiven, ...):
# TANGO_DB_SSL=true

# Connection pool size:
# TANGO_DB_POOL_SIZE=10

# Internal serverless functions (src/apps/*/functions/). Locally they run
# in-process; on Vercel they dispatch over a signed internal channel and the
# secret is required (`vercel env add TANGO_FUNCTIONS_SECRET`):
# TANGO_FUNCTIONS_SECRET=generate-a-long-random-string

# Exercise the production (http) dispatch path locally:
# TANGO_FUNCTIONS_TRANSPORT=http
# TANGO_FUNCTIONS_URL=http://127.0.0.1:8000
